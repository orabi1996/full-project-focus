# مراجعة HRMS وخطة التنفيذ — 10 سبتمبر 2026

## نقطة البداية وحدود العمل

- المستودع: `orabi1996/full-project-focus`؛ أساس المراجعة `c578fa968e28a70ba46282c3d910fd87a47fe3d5` على `main`.
- فرع العمل: `codex/hrms-audit-20260910`، في نسخة مستقلة ونظيفة. لم تُمس نسخ العمل السابقة.
- GitHub يتيح القراءة والرفع؛ `main` غير محمي ولا توجد rulesets عند بدء المراجعة. لم يُختبر الرفع حتى مرحلة التسليم.
- يوجد PR #3 للرواتب (متعارض)، وPR #4 لاكتشاف المنتج وأساس تعدد العملاء (مفتوح). ليس أي منهما دليلًا على وجود التغييرات في `main`.
- React 19 + TypeScript + TanStack Start/Router + Vite 8، Supabase/PostgreSQL، Tailwind/MUI/Radix، Vitest وPlaywright.
- تثبيت `npm ci` والبناء وTypeScript ناجحة. 78 اختبارًا في 15 ملفًا ناجحًا؛ التغطية 97.37% تخص قائمة من 8 ملفات فقط، وليست تغطية النظام كله. Lint: صفر أخطاء و219 تحذيرًا سابقًا.
- آخر GitHub Actions على الأساس: البناء والوحدات ناجحة، والمتصفح فاشل (6 فشل، 1 متذبذب، 3 تخطٍ، 8 نجاح).
- لا تُطبّق migrations على قاعدة فعلية، ولا نشر إنتاجي، ولا إعادة كتابة لتاريخ Git. جميع اختبارات SQL تستخدم بيانات اصطناعية محلية.

## سجل المراحل

| المرحلة | الهدف والتعديل | معيار القبول | الحالة |
|---|---|---|---|
| 0 | حصر الملفات ومراجعة الوحدات وربط النتائج بالمسارات | سجل تغطية لكل ملف وتصنيف جميع الوحدات | جارٍ |
| 1 | منع منح المشرف حسب البريد ومنع الموظف من تغيير سجله الإداري | اختبارات SQL بأدوار قاعدة البيانات، مع بقاء قراءة الموظف وكتابة HR | مكتمل محليًا: 12 اختبار SQL ناجح |
| 2 | الحفاظ على بيانات الموظفين والحضور وفصل التحميل الحقيقي عن Demo | اختبارات قيم صفرية/ناقصة ومقاييس حضور وجلسات متغيرة | منفذ محليًا؛ 7 اختبارات تحويل ناجحة، اختبار المتصفح ينتظر CI |
| 3 | إصلاح أعطال الدخول والتبويبات والتحقق من المدخلات العامة | نجاح المتصفح على سطح المكتب والجوال واختبارات API المناسبة | منفذ محليًا؛ ينتظر CI للمتصفح |
| 4 | استكمال تقرير الوحدات والتحقق النهائي والرفع وPR | فحوصات محلية وCI، ثم دمج عادي إن سمحت القواعد | بانتظار |

## قرارات الإصلاح الأمني

`20260903110500_fix_admin_roles_and_employee_update_policy.sql` يمنح صلاحيات عليا حسب جزء من البريد ويتيح للموظف كتابة سجله كاملًا. الترحيل الإضافي `20260910100000_restore_employee_access.sql` يعيد إنشاء الحساب بدور `employee` فقط ويحصر كتابة الموظفين في HR.

لا يزيل الترحيل الصلاحيات الممنوحة سابقًا تلقائيًا، ولا يصحح الرواتب والهويات التي كتبتها migrations سابقة؛ يلزم مراجعة سجلات فعلية وتحديد أصحاب الصلاحيات المعتمدين قبل أي معالجة. لا يُربط حساب جديد بسجل موظف موجود لمجرد تطابق بريد التسجيل غير المتحقق منه. القيم المالية والهوية والتواريخ غير المعروفة لا تُختلق.

## نقطة الاستئناف الحالية

تنفيذ المرحلة الأولى مع اختبارات PostgreSQL محلية، ثم استكمال مراجعة الوحدات وتصحيح تحويل البيانات. سيُحدّث هذا الملف بالنتائج والأعمال المؤجلة وأسبابها قبل التسليم.

## تحقق المرحلة الأولى

نجح 12 اختبارًا ينفذ SQL فعليًا في PGlite، بما فيها إنشاء حساب ببريد شبيه بالمشرف وبيانات دور مزورة، منع تعديل الراتب والحذف والإدراج الذاتي، بقاء كتابة HR، منع ربط حساب بسجل موجود بالبريد فقط، ومنع سياسة permissive إضافية من إعادة فتح التعديل. الاختبار يعيد تشغيل migrations الأساسية الفعلية ثم الترحيل المسبب للمشكلة والإصلاح.

إعادة تشغيل سلسلة migrations الكاملة توقفت قبل التعديل عند غياب `leave_balances.year` في `20260901080617...sql`. إضافة العمود في تجربة محلية مؤقتة كشفت غياب `approval_steps` أيضًا. أُعيد الملف التاريخي كما كان؛ إنشاء مخطط موافقات غير موثق ليس إصلاحًا آمنًا. يلزم مطابقة مخطط Supabase الفعلي وتسوية سجل migrations في بيئة اختبار. هذا القيد لا يُحسب اختبارًا ناجحًا ولا تثبت اختبارات المرحلة عزل العملاء أو اكتمال المخطط.

## تحديث 12 سبتمبر — المرحلة الثانية

رُفعت المرحلة الأولى إلى GitHub في `78e241864b15587388890a6b2e9fde08881da382` عبر الاتصال المصرح، بعد تعذر اعتماد Git في الطرفية. طابقت شجرة ملفات Commit البعيد الشجرة المحلية حرفيًا.

أزيلت القيم المختلقة من تحويل الموظفين والحضور ومن أغلب الحقول الشخصية في عرض الملف، مع الحفاظ على الرواتب الصفرية وإجمالي الراتب المحفوظ عند تعديل معلومات غير مالية. أصبح ربط الحساب بالموظف عبر `user_id` فقط. تُعاد تهيئة AppProvider عند تغيير هوية الحساب أو Demo، وتُعزل ذاكرة إعدادات الصلاحيات بحسب الحساب؛ هذا لا يحول إعدادات الواجهة إلى صلاحيات مفروضة في قاعدة البيانات.

تُحمّل اللقطتان الأساسية والتشغيلية قبل تحديث الحالة، وتُتجاهل الاستجابات القديمة. خطأ التحميل يمنع عرض الوحدات ويتيح إعادة المحاولة. ربط الدور منفصل عن إشعار Auth المتزامن مع إلغاء النتائج القديمة. حفظ المنشأة يستخدم مسارًا واحدًا ويتحقق من وجود الصف المحفوظ؛ لا يعرض نجاحًا إذا رفضت العملية.

مستودع الوثائق مستقل ويستخدم useState محليًا؛ أصبح عرضه التجريبي منفصلًا عن الحسابات الحقيقية مع بيان عدم توفر الحفظ الحقيقي. لم يُنشأ مخزن ملفات أو مسار اعتماد جديد دون مخطط تخزين موثق.

## تحقق المرحلة الثالثة

- أصبحت كلمة المرور الحالية تتحقق عبر Auth قبل تحديث الجديدة؛ فشل الشبكة أو المزود لا ينتج نجاحًا. إنهاء الجلسات الأخرى يستدعي Auth فعليًا ويبين أن رموز الدخول الحالية تستمر حتى انقضائها. أزيلت حالة MFA والأجهزة والإشعارات المختلقة؛ إعدادها الكامل ما زال غير متاح.
- أُصلح إنهاء حالة الانتظار عند فشل الدخول، ومحدد كلمة المرور في E2E، وتمرير التبويبات أفقيًا على الجوال.
- تحقق Zod من نوع البصمة والتاريخ ذي المنطقة الزمنية والإحداثيات قبل فتح العميل ذي الامتيازات. لا تكشف استجابة فشل الإدراج رسالة قاعدة البيانات. احتساب الحضور يقتصر على البصمات المعتمدة.
- جرى ترميز النصوص المدخلة في تقارير HTML وطباعة الرواتب والوثائق لمنع إدراج وسوم تنفيذية.
- محليًا: TypeScript ناجح، 121 اختبارًا في 20 ملفًا ناجحًا، Lint صفر أخطاء و216 تحذيرًا (الأساس 219). التغطية 97.41% للسجلات المحددة من 11 ملفًا، وليست تغطية كل المشروع. 12 من الاختبارات تنفذ PostgreSQL الفعلي في PGlite.
- لا يتوفر Chromium محليًا وفشل تنزيله بسبب مهلة اتصال الشبكة؛ اختبارات المتصفح المحلية لم تنجح لهذا السبب. ستُسجّل نتيجة CI منفصلة بعد الرفع؛ اختبارات الحساب الحقيقي تستخدم ردود Auth/Data اصطناعية ولا تكتب في قاعدة فعلية.
