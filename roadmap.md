# خارطة طريق HRMS

## مكتمل
- المرحلة 1: ربط كل عمليات الكتابة بقاعدة البيانات عبر `persistLiveChange` (كانت مطبقة من تحديث المستودع).
- المرحلة 2: محرك منطق الأعمال الخادمي
  - `payroll.functions.ts` — تشغيل المسيّر من البيانات الحية + قفل/صرف + تحديث أقساط السلف.
  - `settlement.functions.ts` — مكافأة نهاية الخدمة + بدل الإجازات − السلف القائمة.
  - `attendance.functions.ts` — تحويل البصمات إلى سجلات حضور (ساعات/تأخير/إضافي).
  - `leave.functions.ts` — الاستحقاق الشهري + تسوية الرصيد المحجوز.
  - `approvals.functions.ts` — محرك سلاسل الاعتماد + السجل الزمني + الإشعارات.
  - قيود قاعدة بيانات لمنع التكرار + فهارس أداء.
- المرحلة 4 (جزئي): مساحة تخزين خاصة `hrms-documents` بصلاحيات محكمة.

## متبقٍ
- [ ] واجهة رفع/عرض المستندات من مساحة التخزين (عقود، هويات، إيصالات، سير ذاتية) + تنبيهات انتهاء الصلاحية.
- [ ] المرحلة 3: تشديد إخفاء الوحدات والأزرار حسب الدور في كل الشاشات.
- [ ] ربط شاشة الخدمة الذاتية بحساب الموظف الحالي بالكامل (طلباته/رصيده/بصماته فقط).
- [ ] تصدير PDF لكشوف الرواتب والشهادات الرسمية (CSV متاح حاليًا).
- [ ] جدولة الاستحقاق الشهري وترحيل الحضور آليًا (cron).
